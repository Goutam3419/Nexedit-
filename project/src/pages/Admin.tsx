// Volume 10 (Admin Panel) ka starting point.
// Aage: Users, Templates, Assets, AI usage, Billing, Coupons, Reports, Roles yahan add honge.
export default function AdminPage() {
  return (
    <div className="min-h-screen p-8">
      <h1 className="font-display text-2xl font-semibold mb-4">Admin Panel</h1>
      <p className="text-white/50">Users, templates, billing aur analytics yahan manage honge.</p>
    </div>
  );
}
